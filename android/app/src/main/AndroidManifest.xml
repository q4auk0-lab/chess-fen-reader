<?xml version="1.0" encoding="utf-8"?>
<!-- android/app/src/main/AndroidManifest.xml -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.chessfen.app">

    <!-- ─── İzinler ─────────────────────────────────────────────────────── -->

    <!-- Ekran yakalama (MediaProjection - runtime'da istenir) -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PROJECTION" />

    <!-- Overlay pencere (sonuçları diğer uygulamaların üstünde göstermek için) -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <!-- Ağ (opsiyonel - gelecekte bulut analizi için) -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Panoya erişim -->
    <uses-permission android:name="android.permission.CLIPBOARD_SERVICE" />

    <!-- Titreşim (bildirim için) -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- Önyükleme sonrası servis yeniden başlatma -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:label="Chess FEN Reader"
        android:name="${applicationName}"
        
        android:allowBackup="false"
        android:hardwareAccelerated="true">

        <!-- Ana Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">

            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- MediaProjection Foreground Service -->
        <service
            android:name=".ScreenCaptureService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="mediaProjection" />

        <!-- Flutter plugin meta -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

    </application>
</manifest>

